MEMORY {
    ZP:         start = $0000, size = $0100, type = rw;
    RAM:        start = $0300, size = $7D00, fill = no, type = rw;
}

SEGMENTS {
    ZEROPAGE:   load = ZP, type = zp;
    CODE:       load = RAM, type = ro;
    BSS:        load = RAM, type = bss, define = yes;
}
