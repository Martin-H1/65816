We have identified three classes of routines and each class has it own best
coding practices:

1. Simple loops needing only a single scratch PHY/PLY with Y as counter.
2. Simple buffer walks with addr and count, use (offset,S),Y with X as counter.
3. Complex routines needing indirect addressing through locals, multiple
temporaries, or INC/DEC on locals. Use a full PHD/TCD stack frame.

Prefer the lightweight PHY/PLY only with Y as a counter for simple loops with
no indirect memory access. See SPACES_CODE for a good example.

For simple buffer iteration with an address and count, prefer pushing addr and
count onto the hardware stack and using (offset,S),Y with Y as the byte index
and X as a decrement counter, avoiding a full stack frame entirely. See the
routine TYPE_CODE for a good example of this pattern.

For case 3, use PHD/TCD to point the direct page register at the stack frame
so locals can use direct page addressing modes including INC, DEC, and indirect
(loc).

Do not use SCRATCH0, SCRATCH1, TMPA, TMPB, or W — all temporaries must live in
the stack frame.

Since DP is relocated, all parameter stack accesses via X must use absolute
forced addressing, e.g. LDA a:0,X.

Reserve stack space with TSC / SEC / SBC #SIZE / TCS before TCD, and
save/restore PHD,PHY/PLY,PLD around the frame.

Reserve the full PHD/TCD stack frame pattern for routines that genuinely need
indirect addressing through locals, multiple temporaries, or INC/DEC on local
variables. Don't use a sledgehammer when a tack hammer will do.

All zero page variables (UP, W, etc.) must be accessed before TCD, or via
absolute forced addressing after TCD. Any indirect access through a zero page
pointer after TCD must first load the pointer value into a stack frame local
using absolute forced addressing, then use that local as the indirect base.

A corollary is that frame setup should fetch any zero page values needed
throughout the routine — like UP for user area access — into locals immediately
after reserving the frame but before TCD, following the same pattern
NUMBER_CODE uses for BASE.

Use a single @return label with BRA from success and fall-through from failure,
with stack teardown, PLY, and PLD done once. Specifically, the @return label
must encompass the result push and all stack/register teardown; success and
failure paths must BRA @return or fall through into it, not perform any work
after the label.

Note: The result must be pushed to the parameter stack before stack frame
teardown, because TSC clobbers A. The order must always be:

1. DEX / DEX — make room on parameter stack
2. STA a:0,X — store result
3. TSC / CLC / ADC / TCS — tear down frame
4. PLY — restore IP
5. PLD — restore DP
6. NEXT

A is not clobbered by SEP/REP transitions, so don't insert redundant reloads
after mode switches.

Y is available as a scratch register within a routine where IP is already saved
in the stack frame, so the model doesn't add unnecessary PHY/PLY pairs around
short Y usage.

Do not use numeric literals for character codes; use named constants such as
C_RETURN, BKSP, DEL, SPACE, L_FEED which are defined in constants.inc.

Flags set by the last operation should be exploited where possible — e.g. "use
TAY / BEQ rather than a separate load to test a value that is already available
or can be transferred cheaply."

An example of these techniques is in the sample coding style module:
https://raw.githubusercontent.com/Martin-H1/65816/refs/heads/main/Forth816/Claude/sample_coding_style.s
