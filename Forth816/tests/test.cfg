MEMORY {
    ZP:         start = $0000, size = $0100, type = rw;
    RAM:        start = $0600, size = $7A00, fill = no, type = rw;
}

SEGMENTS {
    ZEROPAGE:   load = ZP, type = zp;
    CODE:       load = RAM, type = ro, align=$100;
    BSS:        load = RAM, type = bss, define = yes;
}
